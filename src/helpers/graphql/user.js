
// import { listUsers } from "../../graphql/queries";

const findUserByCognitoID = async subId => {
//   const users = await API.graphql(
//     graphqlOperation(listUsers, {
//       limit: 100000,
//       filter: { cognitoUserId: { eq: subId } }
//     })
//   );
//   const {
//     data: { listUsers: filteredUsers }
//   } = users;
//   return filteredUsers.items.length > 0 ? filteredUsers.items[0] : null;
};

export { findUserByCognitoID };
